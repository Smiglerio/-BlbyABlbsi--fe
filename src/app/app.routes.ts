import { Routes } from '@angular/router';
import { FormularDemoComponent } from "./formular-demo/formular-demo.component";

export const routes: Routes = [
  { path: 'formular-demo', component: FormularDemoComponent }
];
